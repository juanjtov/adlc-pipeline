# Context log — {{PROJECT_NAME}}

Why each line of persistent context exists. An entry earns a place only after the model got
it wrong **more than once across separate sessions**, and a smart model reading the repo
couldn't have figured it out. Run the `ablation` skill to reset; this file is the record of
what was re-earned vs cargo cult. (See also the keep/cut test in the `ablation` skill.)

<!-- Seed entry: the ADLC context installed at adoption is itself subject to ablation.
     At the next reset, test each generated line against the keep/cut test before keeping it. -->

| Date | Layer (CLAUDE.md / skill / hook / tool) | Line or rule | Failure that justified it | Re-earned? |
|---|---|---|---|---|
| {{DATE}} | — | (ADLC installed by bootstrap wizard) | Pipeline adoption, not a failure patch | — |
